name: pyne_dog_breeds
version: "1.0.0"
config-version: 2

profile: pyne_dog_breeds

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  pyne_dog_breeds:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
